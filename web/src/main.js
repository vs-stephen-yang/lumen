// Entry point for the Lumen web sender.
// Filled in at S6.

const app = document.getElementById('app');
if (app) {
    app.querySelector('p').textContent = 'Scaffolding placeholder — wire up at S6.';
}
